# fe-business-logic

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fe-business-logic` to build the library.

## Running unit tests

Run `nx test fe-business-logic` to execute the unit tests via [Jest](https://jestjs.io).
